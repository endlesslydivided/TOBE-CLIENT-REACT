import { Box, CircularProgress, Container, LinearProgress } from '@mui/material';

const LineLoader = () => {
  return (
      <Box sx={{width:"100%"}}>
        <LinearProgress  />
      </Box>
  );
};

export default LineLoader;

